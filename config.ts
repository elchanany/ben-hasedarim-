// This flag controls whether the app uses the live Firebase backend or the local mock services.
// To use Firebase:
// 1. Set this to `true`.
// 2. Ensure your Firebase project is configured in `src/firebaseConfig.ts`
//    (preferably using a .env file with VITE_... variables).
export const USE_FIREBASE_BACKEND = false;
